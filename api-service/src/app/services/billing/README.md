# Billing Domain

Future home for `billing_service`, `billing_events`, and `payment_gateway`. This folder groups tenant subscription orchestration plus payment adapters; Stripe-specific code lives in the nested `stripe/` package.
