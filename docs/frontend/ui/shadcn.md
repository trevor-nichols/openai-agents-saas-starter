# Shadcn React Hooks

*   React useBoolean Hook
*   React useClickAnyWhere Hook
*   React useCopyToClipboard Hook
*   React useCountdown Hook
*   React useCounter Hook
*   React useDarkMode Hook
*   React DebounceCallback Hook
*   React useDebounceValue Hook
*   React useDocumentTitle Hook
*   React useEventCallback Hook
*   React useEventListener Hook
*   React useHover Hook
*   React IntersectionObserver Hook
*   React useInterval Hook
*   React useIsClient Hook
*   React useIsMounted Hook
*   React useIsomorphicLayout Hook
*   React useLocalStorage Hook
*   React useMap Hook
*   React useMediaQuery Hook
*   React useMousePosition Hook
*   React useOnClickOutside Hook
*   React useReadLocalStorage Hook
*   React useResizeObserver Hook
*   React useScreen Hook
*   React useScript Hook
*   React useScrollLock Hook
*   React useSessionStorage Hook
*   React useStep Hook
*   React useTernaryDarkMode Hook
*   React useTimeout Hook
*   React useToggle Hook
*   React useUnmount Hook
*   React useWindowSize Hook

# Shadcn React Components Installed

.
├── accordion.tsx              # UI component for a vertically stacked set of interactive headings.
├── alert-dialog.tsx           # UI component for a modal dialog that interrupts the user and requires an action.
├── alert.tsx                  # UI component for displaying important, short messages.
├── aspect-ratio.tsx           # UI component for maintaining the aspect ratio of an element.
├── avatar.tsx                 # UI component for displaying a user's image or initials.
├── badge.tsx                  # UI component for displaying small status descriptors.
├── breadcrumb.tsx             # UI component for displaying a navigation trail.
├── button.tsx                 # A versatile button component with different variants and sizes.
├── card.tsx                   # UI component for grouping related content in a container.
├── checkbox.tsx               # A checkbox form input component.
├── command.tsx                # UI component for a command menu (e.g., Spotlight search).
├── context-menu.tsx           # A menu that appears on right-click.
├── dialog.tsx                 # A modal dialog window component.
├── dropdown-menu.tsx          # A menu that appears when a trigger is clicked.
├── form.tsx                   # Helper components for integrating `react-hook-form`.
├── hover-card.tsx             # A card that appears when hovering over a trigger element.
├── input.tsx                  # A standard text input field component.
├── label.tsx                  # A label component for form inputs.
├── pagination.tsx             # UI components for navigating through pages of content.
├── progress.tsx               # UI component for displaying a progress bar.
├── radio-group.tsx            # A set of radio button inputs.
├── resizable.tsx              # UI components for creating resizable panels.
├── scroll-area.tsx            # A container with stylized scrollbars.
├── select.tsx                 # A dropdown select input component.
├── shape-landing-hero.tsx     # A complex hero section component with animated geometric shapes.
├── sheet.tsx                  # A dialog that slides in from the side of the screen.
├── skeleton.tsx               # A placeholder component to indicate loading content.
├── sonner.tsx                 # A toast notification component wrapper for the `sonner` library.
├── switch.tsx                 # A toggle switch input component.
├── table.tsx                  # Components for creating styled data tables.
├── tabs.tsx                   # UI component for switching between different views.
├── textarea.tsx               # A multi-line text input component.
├── toggle.tsx                 # A two-state button that can be toggled on or off.
├── tooltip.tsx                # A pop-up that displays information on hover.
├── separator.tsx              # A component for rendering a horizontal or vertical divider.
├── shadcn-io/                 # Contains custom, complex, or showcase components from shadcn.io.
│   ├── ai/                    # Components specifically for building AI chat interfaces.
│   │   ├── actions.tsx        # Defines action buttons with tooltips, for use in AI messages.
│   │   ├── branch.tsx         # Components for navigating between different AI response branches.
│   │   ├── code-block.tsx     # A client-side syntax-highlighted code block component with a copy button.
│   │   ├── conversation.tsx   # Provides a scrollable container for chat messages with a "stick to bottom" feature.
│   │   ├── image.tsx          # Renders an AI-generated image from various data formats.
│   │   ├── inline-citation.tsx # Displays citations within text, with hover-able details.
│   │   ├── loader.tsx         # A simple SVG spinning loader animation component.
│   │   ├── message.tsx        # Defines the structure for a user or assistant chat message bubble.
│   │   ├── prompt-input.tsx   # A feature-rich text input area for submitting prompts to an AI.
│   │   ├── reasoning.tsx      # A collapsible section to show the AI's "thought process".
│   │   ├── response.tsx       # Renders streaming Markdown responses, handling incomplete tokens.
│   │   ├── source.tsx         # A collapsible section to display the sources an AI used.
│   │   ├── suggestion.tsx     # A horizontal, scrollable list of prompt suggestion chips.
│   │   ├── task.tsx           # A collapsible component to display a task the AI is performing.
│   │   ├── tool.tsx           # A component to visualize the status and I/O of a tool used by an AI.
│   │   └── web-preview.tsx    # An iframe-based component to preview web pages, with navigation controls.
│   ├── animated-beam/         # Contains the Animated Beam component.
│   │   └── index.tsx          # Renders an animated SVG beam between two DOM elements.
│   ├── animated-testimonials/ # Contains the Animated Testimonials component.
│   │   └── index.tsx          # A component for showcasing testimonials with animated transitions.
│   ├── animated-tooltip/      # Contains the Animated Tooltip component.
│   │   └── index.tsx          # A tooltip that animates on hover, typically used for avatars.
│   ├── avatar-group/          # Contains the Avatar Group component.
│   │   └── index.tsx          # Displays a group of overlapping or stacked avatars.
│   ├── code-block/            # Contains an advanced Code Block component.
│   │   ├── index.tsx          # A versatile code block with file selection and client-side highlighting.
│   │   └── server.tsx         # A server-side component for syntax highlighting code using Shiki.
│   ├── copy-button/           # Contains the Copy Button component.
│   │   └── index.tsx          # A button that copies content, with an animated icon change.
│   ├── dropzone/              # Contains the Dropzone component.
│   │   └── index.tsx          # A component for file uploading via drag-and-drop.
│   ├── magnetic/              # Contains the Magnetic component.
│   │   └── index.tsx          # A wrapper that makes its child element "magnetically" follow the cursor.
│   ├── marquee/               # Contains the Marquee component.
│   │   └── index.tsx          # Creates a continuous scrolling effect for its content.
│   ├── navbar-05/             # Contains a specific, feature-rich navbar design.
│   │   └── index.tsx          # A responsive, feature-rich navigation bar component.
│   ├── spinner/               # Contains the Spinner component.
│   │   └── index.tsx          # A loading spinner component with multiple visual style variants.
│   ├── status/                # Contains the Status component.
│   │   └── index.tsx          # Displays a status (e.g., online) with an indicator and label.
│   └── video-player/          # Contains the Video Player component.
│       └── index.tsx          # A video player with custom controls, built on `media-chrome`.