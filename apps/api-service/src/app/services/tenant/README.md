# Tenant Platform Domain

Home for tenant account lifecycle and configuration orchestration services (for example,
`tenant_account_service`, `tenant_lifecycle_service`, and `tenant_settings_service`).
