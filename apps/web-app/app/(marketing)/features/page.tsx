import { FeaturesExperience } from '@/features/marketing';

export const metadata = {
  title: 'Features | Acme',
  description: 'Explore the agent, billing, and operations pillars included in the Acme starter.',
};

export default function FeaturesPage() {
  return <FeaturesExperience />;
}
