API_BASE_URL=http://127.0.0.1:8000            # FastAPI base URL for the BFF
APP_PUBLIC_URL=http://localhost:3000          # Public site URL (SEO canonical)
NEXT_PUBLIC_AGENT_API_MOCK=false              # true = bypass backend, use mock tokens
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=           # Stripe Elements publishable key
NEXT_PUBLIC_LOG_LEVEL=info                    # optional: debug|info|warn|error
NEXT_PUBLIC_LOG_SINK=console                  # optional: console|beacon|none
AGENT_FORCE_SECURE_COOKIES=false              # optional: force Secure cookies (prod=true)
AGENT_ALLOW_INSECURE_COOKIES=false            # optional: allow http cookies in prod
