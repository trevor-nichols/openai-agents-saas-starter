id: bff-no-api-base-url-env
language: TypeScript
severity: error
message: BFF routes must not read API base URLs from env; use server services instead.
rule:
  pattern: "process.env.$VAR"
constraints:
  VAR:
    regex: "^(API_BASE_URL|NEXT_PUBLIC_API_BASE_URL)$"
