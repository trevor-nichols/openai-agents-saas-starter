Operator reports
================

This directory stores CLI-generated artifacts:
- `operator-dashboard.json` — machine-readable doctor output (schema: `starter_contracts/doctor_v1.json`)
- `operator-dashboard.md` — human-readable doctor summary

Artifacts are overwritten on each run. Commit them only for debugging or test fixtures; otherwise they should remain untracked.***
