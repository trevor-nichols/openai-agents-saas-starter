import { PricingExperience } from '@/features/marketing';

export const metadata = {
  title: 'Pricing | Acme',
  description: 'Compare Acme plans and choose the tier that matches your AI agent SaaS rollout.',
};

export default function PricingPage() {
  return <PricingExperience />;
}
