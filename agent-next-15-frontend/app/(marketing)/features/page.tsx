import { FeaturesExperience } from '@/features/marketing';

export const metadata = {
  title: 'Features | Anything Agents',
  description: 'Explore the agent, billing, and operations pillars included in the Anything Agents starter.',
};

export default function FeaturesPage() {
  return <FeaturesExperience />;
}
