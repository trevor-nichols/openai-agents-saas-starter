import { DocsExperience } from '@/features/marketing';

export const metadata = {
  title: 'Docs | Anything Agents',
  description: 'Starter CLI runbooks, auth + billing guides, and ops checklists for Anything Agents.',
};

export default function DocsPage() {
  return <DocsExperience />;
}
