import { PricingExperience } from '@/features/marketing';

export const metadata = {
  title: 'Pricing | Anything Agents',
  description: 'Compare Anything Agents plans and choose the tier that matches your AI agent SaaS rollout.',
};

export default function PricingPage() {
  return <PricingExperience />;
}
